"use client";
import { FileText, UploadCloud } from "lucide-react";
import { useRef, useState } from "react";
const valid = ["application/pdf", "image/jpeg", "image/png"];
export function UploadCard({ onSelect }: { onSelect: (file: File) => void }) { const ref = useRef<HTMLInputElement>(null); const [error, setError] = useState(""); const pick = (file?: File) => { if (!file) return; if (!valid.includes(file.type)) return setError("Upload a PDF, PNG, JPG, or JPEG file."); if (file.size > 10 * 1024 * 1024) return setError("The file must be smaller than 10 MB."); setError(""); onSelect(file); }; return <section onDragOver={(e) => e.preventDefault()} onDrop={(e) => { e.preventDefault(); pick(e.dataTransfer.files[0]); }} className="rounded-3xl border-2 border-dashed border-sky-200 bg-white/70 p-9 text-center shadow-sm"><input ref={ref} className="hidden" type="file" accept="application/pdf,image/png,image/jpeg,.pdf,.png,.jpg,.jpeg" onChange={(e) => pick(e.target.files?.[0])} /><span className="mx-auto grid size-16 place-items-center rounded-2xl bg-gradient-to-br from-sky-600 to-cyan-500 text-white"><UploadCloud className="size-7" /></span><h2 className="mt-5 text-lg font-bold">Upload a medical report</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-slate-500">Drop a blood test, pathology report, or clear report image here.</p><div className="mt-5 flex flex-col justify-center gap-3 sm:flex-row"><button onClick={() => ref.current?.click()} className="rounded-xl bg-sky-600 px-4 py-3 text-sm font-bold text-white"><FileText className="mr-2 inline size-4" /> Upload PDF</button><button onClick={() => ref.current?.click()} className="rounded-xl border border-sky-200 bg-white px-4 py-3 text-sm font-semibold text-sky-700">Upload image</button></div><p className="mt-4 text-xs text-slate-500">PDF, PNG, JPG or JPEG · maximum 10 MB</p>{error && <p role="alert" className="mt-3 text-sm text-rose-600">{error}</p>}</section>; }
