"use client";
import { Eye, Trash2 } from "lucide-react";
import type { SavedReport } from "@/types/report";
export function RecentReports({ reports, onOpen, onDelete }: { reports: SavedReport[]; onOpen: (report: SavedReport) => void; onDelete: (id: string) => void }) { return <section className="rounded-2xl border border-sky-100 bg-white/75 p-5 shadow-sm"><h2 className="font-bold text-slate-900">Recent reports</h2><div className="mt-3 space-y-2">{reports.length ? reports.map((report) => <div key={report.id} className="flex items-center gap-2 rounded-xl border border-sky-100 p-3"><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{report.reportType || "Medical report"}</p><p className="text-xs text-slate-500">{report.createdAt?.toLocaleDateString() ?? "Saving..."} · {report.healthScore}/100</p></div><button onClick={() => onOpen(report)} aria-label="Open report" className="text-sky-600"><Eye className="size-4" /></button><button onClick={() => onDelete(report.id)} aria-label="Delete report" className="text-rose-600"><Trash2 className="size-4" /></button></div>) : <p className="py-5 text-center text-sm text-slate-500">No saved reports yet.</p>}</div></section>; }
