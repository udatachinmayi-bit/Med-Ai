import type { ReportAnalysis } from "@/types/report";
export function TestResultsTable({ tests }: { tests: ReportAnalysis["tests"] }) { return <section className="overflow-hidden rounded-2xl border border-sky-100 bg-white/75 shadow-sm"><h2 className="p-5 font-bold text-slate-900">Test results</h2>{tests.length ? <div className="overflow-x-auto"><table className="w-full min-w-[620px] text-left text-sm"><thead className="bg-sky-50 text-slate-500"><tr><th className="px-5 py-3">Test</th><th>Value</th><th>Range</th><th>Status</th><th className="px-5">Significance</th></tr></thead><tbody>{tests.map((test, index) => <tr key={`${test.name}-${index}`} className="border-t border-sky-100"><td className="px-5 py-3 font-semibold text-slate-800">{test.name}</td><td>{test.value} {test.unit}</td><td>{test.normalRange}</td><td><span className={test.status === "Normal" ? "text-emerald-700" : "text-rose-700"}>{test.status}</span></td><td className="px-5 py-3">{test.clinicalSignificance || test.reason}</td></tr>)}</tbody></table></div> : <p className="px-5 pb-5 text-sm text-slate-500">No individual test results were identified.</p>}</section>; }
