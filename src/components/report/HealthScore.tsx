export function HealthScore({ score }: { score: number }) { const color = score >= 75 ? "text-emerald-600" : score >= 45 ? "text-amber-600" : "text-rose-600"; return <section className="rounded-2xl border border-sky-100 bg-white/75 p-5 text-center shadow-sm"><p className="text-sm font-semibold text-slate-600">Health score</p><p className={`mt-2 text-5xl font-bold ${color}`}>{score}<span className="text-lg">/100</span></p><div className="mt-4 h-2 overflow-hidden rounded-full bg-sky-100"><div className="h-full rounded-full bg-sky-600" style={{ width: `${score}%` }} /></div></section>; }
