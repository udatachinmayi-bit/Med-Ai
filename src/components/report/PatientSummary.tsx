import type { ReportAnalysis } from "@/types/report";
export function PatientSummary({ analysis }: { analysis: ReportAnalysis }) { return <section className="rounded-2xl border border-sky-100 bg-white/75 p-5 shadow-sm"><h2 className="font-bold text-slate-900">Patient & report details</h2><div className="mt-3 grid gap-2 text-sm text-slate-600 sm:grid-cols-2"><p><b>Name:</b> {analysis.patient.name || "Not provided"}</p><p><b>Age / gender:</b> {[analysis.patient.age, analysis.patient.gender].filter(Boolean).join(" · ") || "Not provided"}</p><p><b>Report:</b> {analysis.report.type || "Not clear"}</p><p><b>Laboratory:</b> {analysis.report.laboratory || "Not clear"}</p></div></section>; }
