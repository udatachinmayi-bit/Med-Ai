import { FileText, Trash2 } from "lucide-react";
export function ReportPreview({ file, onReplace, onRemove }: { file: File; onReplace: () => void; onRemove: () => void }) { return <section className="flex items-center gap-4 rounded-2xl border border-sky-100 bg-white/75 p-4 shadow-sm"><span className="grid size-12 place-items-center rounded-xl bg-sky-50 text-sky-600"><FileText /></span><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold text-slate-800">{file.name}</p><p className="mt-1 text-xs text-slate-500">{file.type === "application/pdf" ? "PDF report" : "Report image"} · {(file.size / 1024 / 1024).toFixed(2)} MB</p></div><button onClick={onReplace} className="text-sm font-semibold text-sky-700">Replace</button><button onClick={onRemove} aria-label="Remove report" className="text-rose-600"><Trash2 className="size-4" /></button></section>; }
