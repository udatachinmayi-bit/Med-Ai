"use client";
import { motion, useReducedMotion } from "framer-motion";
import { Brain, FileSearch, HeartPulse, Upload } from "lucide-react";
import { SectionBadge } from "../ui/SectionBadge";
import { SectionHeading } from "../ui/SectionHeading";
const steps = [{ icon: Upload, title: "Share", text: "Upload a report, scan a medicine, or describe what you are feeling." }, { icon: Brain, title: "Understand", text: "Our AI organises the information into clear, thoughtful context." }, { icon: FileSearch, title: "Explore", text: "Review key insights, questions, and practical next steps at your pace." }, { icon: HeartPulse, title: "Move forward", text: "Save your understanding and have better conversations with your care team." }];
export default function HowItWorks() { const reduce = useReducedMotion(); return <section className="relative overflow-hidden bg-slate-50 py-20 sm:py-24 lg:py-32" id="how-it-works"><div className="mx-auto max-w-7xl px-5 sm:px-6"><SectionHeading align="center" badge={<SectionBadge>Simple by design</SectionBadge>} subtitle="A clearer care journey" title="From uncertainty to understanding in four calm steps." description="No complex setup. Just the right information, presented when you need it." className="mx-auto" /><div className="relative mt-14 grid gap-5 sm:grid-cols-2 lg:mt-20 lg:grid-cols-4 lg:gap-4"><div aria-hidden="true" className="absolute left-[12%] right-[12%] top-10 hidden h-px bg-gradient-to-r from-transparent via-sky-300 to-transparent lg:block" />{steps.map(({ icon: Icon, title, text }, index) => <motion.article key={title} initial={{ opacity: 0, y: reduce ? 0 : 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * .1, duration: .45 }} whileHover={reduce ? undefined : { y: -6 }} className="relative rounded-3xl border border-white/80 bg-white/75 p-6 shadow-[0_14px_35px_rgba(14,116,144,.08)] backdrop-blur-xl"><span className="absolute right-5 top-5 grid size-7 place-items-center rounded-full bg-sky-50 text-xs font-bold text-sky-600">0{index + 1}</span><span className="grid size-14 place-items-center rounded-2xl bg-gradient-to-br from-sky-500 to-cyan-400 text-white shadow-lg shadow-sky-200/60"><Icon className="size-6" /></span><h3 className="mt-6 text-xl font-bold text-slate-950">{title}</h3><p className="mt-3 text-sm leading-6 text-slate-600">{text}</p></motion.article>)}</div></div></section>; }
