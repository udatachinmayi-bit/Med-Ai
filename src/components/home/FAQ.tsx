"use client";
import { useState } from "react";
import { AnimatePresence, motion } from "framer-motion";
import { ChevronDown, ShieldCheck } from "lucide-react";
import { SectionBadge } from "../ui/SectionBadge";
import { SectionHeading } from "../ui/SectionHeading";
const faqs = [{ q: "How does MedAI help me understand a medicine?", a: "Upload a clear image of the medicine or prescription. MedAI organises the available information into a more readable explanation, including helpful precautions and questions to consider." }, { q: "Can I upload medical reports?", a: "Yes. You can upload common medical documents such as blood tests and health reports to receive an approachable summary of the information they contain." }, { q: "Is MedAI a replacement for my clinician?", a: "No. MedAI supports understanding and preparation—it does not diagnose, prescribe, or replace professional medical advice. Always consult a qualified clinician for medical decisions." }, { q: "How is my information handled?", a: "We design the experience around privacy and clear expectations. Sensitive health information should always be handled with appropriate security and healthcare privacy practices." }];
export default function FAQ() { const [open, setOpen] = useState(0); return <section className="bg-white py-20 sm:py-24 lg:py-32" id="resources"><div className="mx-auto grid max-w-6xl gap-10 px-5 sm:px-6 lg:grid-cols-[.7fr_1.3fr] lg:gap-16"><SectionHeading badge={<SectionBadge>Questions, answered</SectionBadge>} subtitle="Helpful to know" title="A few things people ask before they begin." description="Straightforward answers about how MedAI fits into your health journey." /><div className="space-y-3">{faqs.map((faq, index) => <div key={faq.q} className="overflow-hidden rounded-2xl border border-sky-100 bg-sky-50/35"><button aria-expanded={open === index} className="flex w-full items-center justify-between gap-5 px-5 py-5 text-left sm:px-6" onClick={() => setOpen(open === index ? -1 : index)} type="button"><span className="font-semibold tracking-tight text-slate-900">{faq.q}</span><ChevronDown className={`size-5 shrink-0 text-sky-600 transition-transform ${open === index ? "rotate-180" : ""}`} /></button><AnimatePresence initial={false}>{open === index && <motion.div initial={{ height: 0, opacity: 0 }} animate={{ height: "auto", opacity: 1 }} exit={{ height: 0, opacity: 0 }} transition={{ duration: .22 }} className="overflow-hidden"><p className="px-5 pb-5 text-sm leading-7 text-slate-600 sm:px-6 sm:pb-6">{faq.a}</p></motion.div>}</AnimatePresence></div>)}<div className="flex items-center gap-2 px-2 pt-3 text-xs text-slate-500"><ShieldCheck className="size-4 text-emerald-500" /> MedAI supports understanding—not medical diagnosis.</div></div></div></section>; }
