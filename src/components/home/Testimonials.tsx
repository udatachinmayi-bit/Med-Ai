"use client";
import { motion, useReducedMotion } from "framer-motion";
import { Quote, Star } from "lucide-react";
import { SectionBadge } from "../ui/SectionBadge";
import { SectionHeading } from "../ui/SectionHeading";
const testimonials = [{ name: "Sarah Johnson", role: "Member since 2024", initials: "SJ", quote: "I went from feeling lost in my report to knowing exactly what to discuss with my doctor. It made a stressful week feel manageable." }, { name: "Michael Smith", role: "Member since 2023", initials: "MS", quote: "The explanations are clear without being alarming. It gives me a far better starting point for understanding my health information." }, { name: "Emily Davis", role: "Member since 2024", initials: "ED", quote: "Having a calm place to ask questions and organise my health notes has been genuinely helpful." }];
export default function Testimonials() { const reduce = useReducedMotion(); return <section className="relative overflow-hidden bg-slate-50 py-20 sm:py-24 lg:py-32"><div aria-hidden="true" className="absolute left-1/2 top-0 h-52 w-2/3 -translate-x-1/2 rounded-full bg-cyan-100/60 blur-3xl" /><div className="relative mx-auto max-w-7xl px-5 sm:px-6"><SectionHeading align="center" badge={<SectionBadge>Member stories</SectionBadge>} subtitle="Trusted in everyday moments" title="Clearer information can change how care feels." description="Thoughtful tools for the moments when a little more understanding makes a real difference." className="mx-auto" /><div className="mt-12 grid gap-5 lg:mt-16 lg:grid-cols-3 lg:gap-6">{testimonials.map((item, index) => <motion.figure key={item.name} initial={{ opacity: 0, y: reduce ? 0 : 22 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * .1 }} whileHover={reduce ? undefined : { y: -6 }} className="relative rounded-3xl border border-white/80 bg-white/80 p-6 shadow-[0_16px_45px_rgba(14,116,144,.09)] backdrop-blur-xl sm:p-7"><Quote className="absolute right-6 top-6 size-8 text-sky-100" /><div className="flex text-amber-400">{[1, 2, 3, 4, 5].map((star) => <Star key={star} className="size-4" fill="currentColor" />)}</div><blockquote className="mt-5 text-base leading-7 text-slate-700">“{item.quote}”</blockquote><figcaption className="mt-7 flex items-center gap-3 border-t border-sky-50 pt-5"><span className="grid size-10 place-items-center rounded-full bg-gradient-to-br from-sky-100 to-cyan-100 text-xs font-bold text-sky-700">{item.initials}</span><span><span className="block text-sm font-bold text-slate-900">{item.name}</span><span className="block text-xs text-slate-500">{item.role}</span></span></figcaption></motion.figure>)}</div></div></section>; }
